<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.SWEProject.bringwithyou">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activites.HomeProf" >
           <!-- android:label="@string/title_activity_home_prof"
            android:theme="@style/AppTheme.NoActionBar"    -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <!--<activity
            android:name=".Activites.Home2"
            android:label="@string/title_activity_home2"
            android:theme="@style/AppTheme.NoActionBar" /> !-->
        <activity android:name=".Activites.Login">

        </activity>
        <activity android:name=".Activites.Home" />
        <activity android:name=".Activites.Register" />
    </application>

</manifest>